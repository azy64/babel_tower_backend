<div class="row">
<h3>Mes leçons</h3>
{% for item in members %}
{% set lessons = item.classroom.lessons %}
    {% for lesson in lessons %}
        <div class="rounded bg-white mb-2 mt-3 p-3">
            <a class="text-decoration-none" href="#">{{lesson.id}} {{lesson.title}} {{lesson.dateLesson|date('d-m-Y H:m:s')}}</a>
            <hr>
            <div class="row">
                {%  set contents = lesson.contenus %}
                {% for content in contents %}
                   <div class="col contenu" id="contenu{{content.id}}"
                   data-link="{{content.filename}}" 
                   data-type="{{content.typecontenu.libelle}}"
                   data-title="{{content.libelle}}" 
                   data-consigne="{{lesson.consigneOne}}--{{lesson.consigneTwo}}"
                   data-repetition="{{ modal.findBy({"contenu": content.id})[0].lecture.repetition }}" 
                   >
                   <div class="questions">
                   {% if qr.findBy({'contenu': content}) is not empty %}
                   {% for quest in qr.findBy({'contenu': content}) %}
                    <input type="hidden" id="question{{quest.id}}"
                        data-question="{{quest.question.libelle}}"
                        data-student="{{student.id}}"
                        data-id="{{quest.question.id}}"
                        data-reponse="{{quest.question.reponse}}"
                        data-assertion1="{{quest.question.assertion1}}"
                        data-assertion2="{{quest.question.assertion2}}"
                        data-assertion3="{{quest.question.assertion3}}"
                        data-lesson="{{lesson.id}}"
                        data-contenu="{{content.id}}"
                      />
                   {% endfor %}
                   {% endif %}
                   </div>
                
                        {% if content.typecontenu.libelle == "audio"  %}
                            <div class="clicked"> 
                                 <span>{{content.libelle}}</span>
                                <img width="40" src="{{asset('images/audio.png')}}"/>
                            </div>
                        {% else %}
                        <div class="clicked"> 
                            <span>{{content.libelle}}</span>
                            <img width="40" src="{{asset('images/video2.png')}}" />
                        </div>
                        {% endif %}
                   </div> 
                {% endfor %}
            </div>
        </div>
    {% endfor %}
    
{% endfor %}
</div>