{% extends 'base.html.twig' %}

{% block title %}ClassRoom{% endblock %}

{% block body %}
    <h1>ClassRoom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class_room.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ class_room.nom }}</td>
            </tr>
            <tr>
                <th>CreationClass</th>
                <td>{{ class_room.creationClass ? class_room.creationClass|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_class_room_index') }}">back to list</a>

    <a href="{{ path('app_class_room_edit', {'id': class_room.id}) }}">edit</a>

    {{ include('class_room/_delete_form.html.twig') }}
{% endblock %}
